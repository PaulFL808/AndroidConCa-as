<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AsistenciaAlumno">

        <!-- 1. NUEVA PANTALLA INICIAL (P-AL01): Arranca primero -->
        <activity
            android:name=".InicioActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2. PANTALLA DE REGISTRO (P-AL02): El formulario -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- 3. PANTALLA DE CONFIRMACIÓN (P-AL03): El éxito -->
        <activity
            android:name=".ConfirmacionActivity"
            android:exported="false" />

    </application>

</manifest>